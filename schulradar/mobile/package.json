{
  "name": "schulradar-mobile",
  "version": "1.2.2",
  "private": true,
  "description": "Schulradar als Android-App (Capacitor) – nutzt die Anbindungen und die Oberfläche aus ../src",
  "scripts": {
    "build": "node build.mjs",
    "preview": "node build.mjs --preview",
    "sync": "node build.mjs && npx cap sync android",
    "apk": "node build.mjs --minify && npx cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.2",
    "@capacitor/filesystem": "^8.1.3",
    "@capacitor/local-notifications": "^8.3.1",
    "@capacitor/share": "^8.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2",
    "buffer": "^6.0.3",
    "esbuild": "^0.25.0"
  }
}
